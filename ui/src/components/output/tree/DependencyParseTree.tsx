import * as React from "react";
import { Tree } from "hierplane";
import "./override.css";

const DependencyParseTree = (props: any) => {
  return <Tree {...props} />;
};

export default DependencyParseTree;
